# Dotfiles
my dotfiles repo.

it's a bit weird because it uses submodules instead of a monolith... 

run `./setup.sh` to update submodules, install deps and link everything.

run `./update.sh` to just update all the submodules
